<table id="example2" class="table table-bordered table-hover">
    <thead>
    <tr>
      <th>No.</th>
      <th>Nama</th>
      <th>Action</th>
    </tr>
    </thead>
    <tbody>
        <% for(let i=0; i < category.length; i++) { %>
        <tr>
            <td> <%=  i + 1 %></td>
            <td> <%=  category[i].name %></td>
            <td> <form >
                <a href="/category/edit/<%= category[i]._id %>" class="btn btn-info"> <i class=" fas fa-pencil-alt "></i> Ubah</a>
                <a href="/category/delete/<%= category[i]._id %> " class="btn btn-danger" > <i class="fas fa-trash"></i> Hapus</a>
            </form></td>
        </tr>
        <% } %>
    </tbody>
    
  </table>