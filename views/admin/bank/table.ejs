<table id="example2" class="table table-bordered table-hover">
    <thead>
    <tr>
      <th>No.</th>
      <th>Nama</th>
      <th>Bank</th>
      <th>Nomor Rekening</th>
      <th>Action</th>
    </tr>
    </thead>
    <tbody>
        <% for(let i=0; i < bank.length; i++) { %>
        <tr>
            <td> <%=  i + 1 %></td>
            <td> <%=  bank[i].name %></td>
            <td> <%= bank[i].bankName %></td>
            <td> <%= bank[i].nomorRekening %></td>
            <td> <form >
                <a href="/bank/edit/<%= bank[i]._id %>" class="btn btn-info"> <i class=" fas fa-pencil-alt "></i> Ubah</a>
                <a href="/bank/delete/<%= bank[i]._id %> " class="btn btn-danger" > <i class="fas fa-trash"></i> Hapus</a>
            </form></td>
        </tr>
        <% } %>
    </tbody>
    
  </table>